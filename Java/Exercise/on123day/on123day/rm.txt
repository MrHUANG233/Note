纸牌取最大值
先手函数 =取最大值（取左边+左边没了之后后手，取右值+右边没有后的后手）
后手函数 =取小值（左边后右边被取走后，先手函数的最小值）

2.使用hashCode将大文件分组，相同值的分到同一组、记录到小文件中，而不是内存，最后分别计算每组的词频最大的数，释放内存。

40亿个整数的大文件，4G内存，找出出现次数最多的一个数。
不直接使用HashMap
先使用HashCode分组，在各个分组中分别使用HashMap

3.布隆过滤器
黑名单，一个只有增加和查询的操作
允许一定失误率p
大样本量n

m=-(n*ln(p))/(ln2)^2

k=ln2*m/n =0.7*m/n   哈希函数个数（采集的特征数）

实际失误率p=（1-exp（-（n*k）/m真））^k真